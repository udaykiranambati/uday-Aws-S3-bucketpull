name: Hello world

on:
 push:
   branches: main
 workflow_dispatch:


jobs:
  hello:
     runs-on: ubuntu-latest
     steps:
       - uses: actions/checkout@v2
       - name: hello world
         run: echo "hellow world Praveen!"
         shell: bash
